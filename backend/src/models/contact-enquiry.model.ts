/**
 * contact-enquiry.model.ts
 * Kept as a thin re-export alias so any code that imports this path still works.
 * The canonical model is Lead (type = "contact").
 */
import Lead from "./lead.model.js";
export default Lead;
